# TODO

Use this as the short-lived execution checklist companion to `PLAN.md`.

## Current queue

- [x] Confirm phase scope in `PLAN.md`.
- [x] Break work into concrete code/docs tasks.
- [x] Run `cargo fmt -- --check`.
- [x] Run `cargo test`.
- [x] Record handoff with `/handoff <name>`.

## Notes

- Keep items specific and actionable.
- Move completed, phase-level outcomes back into `PLAN.md`.
